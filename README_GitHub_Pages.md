# GitHub公開メモ

このフォルダでは、公開用ページを `docs/index.html` に配置しています。

GitHubで公開する手順:

1. GitHubで新しいリポジトリを作成する
2. このフォルダをそのリポジトリに push する
3. GitHub の `Settings` → `Pages` を開く
4. `Build and deployment` の `Source` を `Deploy from a branch` にする
5. Branch を `master`、Folder を `/docs` にする
6. 保存後、数分で公開URLが発行される

公開対象:

- `docs/index.html`

元ファイル:

- `社内AI活用アンケート_集計レポート_2026-04-16.html`
- `update_ai_report.ps1`
